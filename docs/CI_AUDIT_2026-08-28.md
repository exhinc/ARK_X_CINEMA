# CI Audit Checkpoint — 2026-08-28

This checkpoint intentionally contains no runtime code. It records that the
Engine test suite should be used as the verification gate after the recent
stage-adapter reconciliation.

Required gate:

- `python -m pytest Engine/Tests -q`
- No real movie, Whisper, Ollama, TTS engine, or FFmpeg runtime is required
  for this CI suite.
- Real-machine integration remains a separate validation step.
